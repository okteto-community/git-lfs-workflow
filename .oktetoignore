# We are not synchronizing this directory because Okteto is taking care of this under the deploy.commands with "git lfs pull"
# This allows developers working on the same repo with LFS do not need to download the LFS files, or even have it configured locally
.git/lfs